language: node_js
node_js:
  - "12"
before_script:
  - npm install
script: npm run build
deploy:
  local-dir: dist/gestion-des-transports-groupe1-front
  provider: pages
  skip-cleanup: true
  github-token: $token
  on:
    branch: travis
notifications:
  email: false
